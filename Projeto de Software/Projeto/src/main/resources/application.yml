server:
    port: 8080

spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url:  jdbc:h2:file:~/workspace/estoque-facil/database/database.db
    data-username: sa
    data-password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop